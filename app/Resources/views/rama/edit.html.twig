{% extends 'base_new.html.twig' %}

{% block body %}
    <h1>Rama edit</h1>
<h3>
    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
         <div class="btn-group" role="group" aria-label="Basic example">
            <a href="{{ path('rama_index') }}" button type="button" class="btn btn-outline-primary">Volver</a>
            <a href="{{ path('rama_edit', { 'id': rama.id }) }}" button type="button" class="btn btn-outline-primary">Editar</a>
    {{ form_end(edit_form) }}

   
</h3>
{% endblock %}
