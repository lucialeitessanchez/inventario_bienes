{% extends 'base.html.twig' %}

{% block body %}

    <h1>Tipo creation</h1>
    <h2>
    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('tipo_index') }}">Back to the list</a>
        </li>
    </ul>
</h2>
{% endblock %}
