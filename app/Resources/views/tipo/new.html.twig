{% extends 'base_new.html.twig' %}

{% block body %}

    <h1>Crear un nuevo tipo</h1>
    <h2>
    {% with { 'btnTxt': 'Crear'}%}
  {{ block("formBlock", "./blocks/formBlock.twig") }}
  {% endwith %}


       
            <a href="{{ path('tipo_index') }}" button type="button" class="btn btn-outline-primary">Volver</a>
    
    
</h2>
{% endblock %}
