{% extends 'base_new.html.twig' %}

{% block body %}
    <h1>Users list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Username</th>
                <th>Password</th>
                <th>Email</th>
                <th>Isactive</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for user in users %}
            <tr>
                <td><a href="{{ path('user_show', { 'id': user.id }) }}">{{ user.id }}</a></td>
                <td>{{ user.username }}</td>
                <td> ... {{ user.password | slice( - 15 ) }} </td>
                <td>{{ user.email }}</td>
                <td>{% if user.isActive %}Yes{% else %}No{% endif %}</td>
                <td>
                    <ul>
                        <li>
                            <a class="btn btn-info"  href="{{ path('user_show', { 'id': user.id }) }}">Ver</a>
                        </li>
                        <li>
                            <a class="btn btn-primary"  href="{{ path('user_edit', { 'id': user.id }) }}">Modificar</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a class="btn btn-primary"  href="{{ path('user_new') }}">Crear Nuevo Usuario</a>
        </li>
    </ul>
{% endblock %}
