{% extends 'base_new.html.twig' %}

{% block body %}
    <h1>User creation</h1>

    {% with {'form': form, 'btnTxt': 'Crear'}%}
        {{ block("formBlock", "./blocks/formBlock.twig") }}
    {% endwith %}

    <ul>
        <li>
            <a class="btn btn-primary" href="{{ path('user_index') }}">Listado de Usuarios</a>
        </li>
    </ul>
{% endblock %}
